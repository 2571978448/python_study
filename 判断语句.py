# 赵百超
# 创建时间  2023/9/10  10:55
#  通过比较运算可以得到布尔类型的结果    也可手动定义得到布尔类型的结果
result = 1 == 1
print(result)  # 结果是true

#  if语句后面要加 : ，并且前面要有4个空格缩进
#  else和if同级,缩进一样
#  elif的使用, elif可以写多个，判断是互斥的，上一个满足后面的就不会判断了

#  if判断语句的嵌套(多个if)  ,  关键点是空格缩进来决定层次关系 , 只有上一个if条件满足 , 才会执行下一个if条件
