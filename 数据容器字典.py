# 赵百超
# 创建时间  2023/9/11  15:46

# 字典的定义 {key: value, key: value, key: value, key: value, key: value, key: value,...}
# 空字典 {} 或 dict()
# 不允许Key的重复
# 字典基于key获取value, 不可以下标索引  可以修改
# key和value可以是任意类型, key不可以是字典
# 字典新增元素/更新元素  dict["key"] = value
# 删除元素 dict.pop(key), 获得指定key的value, 同时删除该键值对
# 清空字典 dict.clear()
# 得到字典全部的key dict.keys()
# 字典的遍历 1, 可获取全部key, 再for循环遍历 2, 直接for遍历数据容器, 每一次循环都是直接得到key   * 同样不支持while循环遍历
