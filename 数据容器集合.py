# 赵百超
# 创建时间  2023/9/11  15:18

# 通过大括号{}进行定义      无序, 不允许重复, 允许修改
# 添加元素 set.add()   移除元素 set.remove()   取出元素 set.pop() (不在原集合了)
# 遍历不支持while, 只支持for

# 统计数据容器元素个数 len()
# 容器内最大元素 max()
# 容器最小元素 min()

# 容器的通用转换 list(...)转列表 tuple(...) set(...) str(...)  无法转字典, 字典转别的, 丢失value

# 容器的通用排序 sorted(容器,[reverse=true])  reverse = true 表示降序